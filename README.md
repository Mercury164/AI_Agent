# 🤖 AI Browser Agent

Автономный AI-агент для управления веб-браузером с использованием DeepSeek API.

## ✨ Возможности

- **Автономное выполнение задач** — агент сам определяет последовательность действий
- **Видимый браузер** — наблюдайте за работой агента в реальном времени
- **Persistent sessions** — сохранение сессий браузера (cookies, авторизации)
- **Security layer** — подтверждение опасных действий (удаление, оплата)
- **Умное извлечение DOM** — оптимизация контекста для экономии токенов
- **Обработка ошибок** — автоматическая адаптация при неудачах

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
# Убедитесь, что Go >= 1.21 установлен
go version

# Установите Google Chrome (если не установлен)
# Агент использует Chrome через CDP (Chrome DevTools Protocol)

# Загрузите зависимости
go mod download
```

### 2. Получите API ключ DeepSeek

1. Зарегистрируйтесь на [platform.deepseek.com](https://platform.deepseek.com)
2. Создайте API ключ в разделе API Keys
3. Установите переменную окружения:

```bash
export DEEPSEEK_API_KEY="sk-your-api-key-here"
```

### 3. Запустите агента

```bash
go run .
```

### 4. Откройте веб-интерфейс

Перейдите в браузере на [http://localhost:8080](http://localhost:8080)

## 📝 Примеры задач

### 📧 Управление почтой
```
Прочитай последние 10 писем в яндекс почте и удали спам
```

### 💼 Поиск вакансий
```
Найди 3 вакансии AI-инженера на hh.ru и откликнись на них с сопроводительным письмом
```

### 🍔 Заказ еды
```
Закажи BBQ-бургер и картошку фри на сайте delivery-club.ru
```

### 🔍 Поиск информации
```
Найди на google.com последние новости про искусственный интеллект и составь краткую сводку
```

## 🏗️ Архитектура

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Web UI        │────▶│   Go Server     │────▶│   DeepSeek API  │
│   (browser)     │◀────│   (WebSocket)   │◀────│   (decisions)   │
└─────────────────┘     └────────┬────────┘     └─────────────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │   chromedp      │
                        │   (Chrome CDP)  │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │   Chrome        │
                        │   (visible)     │
                        └─────────────────┘
```

### Компоненты

| Файл | Описание |
|------|----------|
| `main.go` | HTTP сервер, WebSocket, маршрутизация |
| `agent.go` | AI агент, интеграция с DeepSeek, логика принятия решений |
| `static/index.html` | Веб-интерфейс для управления агентом |

## 🛡️ Безопасность

Агент автоматически запрашивает подтверждение для:
- Удаления данных (письма, файлы)
- Оплаты и финансовых операций
- Отправки форм и сообщений
- Подтверждения заказов

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DEEPSEEK_API_KEY` | API ключ DeepSeek | обязательный |
| `PORT` | Порт HTTP сервера | 8080 |

### Параметры браузера

Браузер запускается с параметрами:
- `--headless=false` — видимый режим
- `--user-data-dir=./chrome-user-data` — сохранение сессий
- `--window-size=1400,900` — размер окна

## 🔧 Доступные действия агента

| Действие | Описание |
|----------|----------|
| `navigate` | Переход по URL |
| `click` | Клик по элементу (CSS селектор) |
| `type` | Ввод текста в поле |
| `scroll` | Прокрутка страницы |
| `wait` | Ожидание (мс) |
| `extract` | Извлечение текста |
| `screenshot` | Скриншот страницы |

## 📊 Управление контекстом

Для работы с ограничениями токенов:
1. HTML упрощается (удаляются скрипты, стили, SVG)
2. DOM обрезается до 15000 символов
3. История диалога ограничена 20 сообщениями

## 🐛 Отладка

Логи выводятся в терминал и веб-интерфейс:
- 🤔 **Thinking** — рассуждения агента
- 🎯 **Action** — выполняемые действия
- ⚠️ **Error** — ошибки и проблемы
- ✅ **Complete** — завершение задачи

## 📜 Лицензия

MIT License

---

💡 **Совет**: Перед запуском задачи войдите в нужные аккаунты в браузере агента — сессии сохраняются между запусками!
